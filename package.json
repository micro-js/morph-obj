{
  "name": "@f/morph-obj",
  "description": "Morph an object so that it looks like another object",
  "repository": "git://github.com/micro-js/morph-obj.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
